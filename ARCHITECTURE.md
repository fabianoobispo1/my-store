# 🏗️ Arquitetura Docker - MedusaJS 2.0

Visualização da arquitetura containerizada do projeto.

## 📊 Diagrama de Arquitetura

```
┌─────────────────────────────────────────────────────────────────────┐
│                          USUÁRIO / CLIENTE                          │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                 ┌───────────┴──────────┐
                 │                      │
          ┌──────▼──────┐        ┌─────▼──────┐
          │  Storefront │        │   Admin    │
          │   :8000     │        │  :9000/app │
          └──────┬──────┘        └─────┬──────┘
                 │                     │
                 └──────────┬──────────┘
                            │
┌───────────────────────────▼───────────────────────────────────────┐
│                  MEDUSA NETWORK (Bridge)                          │
│                                                                    │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │                    Backend (MedusaJS)                        │ │
│  │                         :9000                                │ │
│  │                                                              │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │ │
│  │  │   API        │  │  Admin SDK   │  │  Workflows   │     │ │
│  │  └──────────────┘  └──────────────┘  └──────────────┘     │ │
│  │                                                              │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │ │
│  │  │  Subscribers │  │   Modules    │  │    Jobs      │     │ │
│  │  └──────────────┘  └──────────────┘  └──────────────┘     │ │
│  └───────┬────────────┬────────────┬────────────┬────────────┘ │
│          │            │            │            │               │
│  ┌───────▼─────┐ ┌───▼────┐ ┌────▼─────┐ ┌───▼──────────┐    │
│  │ PostgreSQL  │ │ Redis  │ │  MinIO   │ │ MeiliSearch  │    │
│  │   :5432     │ │ :6379  │ │  :9000   │ │    :7700     │    │
│  │             │ │        │ │  :9001   │ │              │    │
│  │ ┌─────────┐ │ │ ┌────┐ │ │ ┌──────┐ │ │ ┌──────────┐ │    │
│  │ │ Volume  │ │ │ │Vol │ │ │ │Volume│ │ │ │ Volume   │ │    │
│  │ └─────────┘ │ │ └────┘ │ │ └──────┘ │ │ └──────────┘ │    │
│  └─────────────┘ └────────┘ └──────────┘ └──────────────┘    │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

## 🔄 Fluxo de Dados

### 1. Requisição do Cliente
```
Cliente → Storefront (Next.js)
  ↓
  Storefront consulta Backend via SDK
  ↓
  Backend (MedusaJS) processa request
  ↓
  ┌─────────────────────────┐
  │ Backend acessa:         │
  │ • PostgreSQL (dados)    │
  │ • Redis (cache)         │
  │ • MinIO (imagens)       │
  │ • MeiliSearch (busca)   │
  └─────────────────────────┘
  ↓
  Backend retorna resposta
  ↓
  Storefront renderiza UI
  ↓
Cliente recebe página
```

### 2. Upload de Mídia
```
Admin Dashboard → Upload de imagem
  ↓
  Backend recebe arquivo
  ↓
  MinIO File Module processa
  ↓
  ┌──────────────────────────┐
  │ 1. Gera ULID único       │
  │ 2. Faz upload no MinIO   │
  │ 3. Salva URL no Postgres │
  └──────────────────────────┘
  ↓
  URL retornada: http://minio:9000/medusa-media/{ulid}.jpg
  ↓
Imagem disponível publicamente
```

### 3. Evento e Notificação
```
Usuário faz pedido no Storefront
  ↓
  Backend cria Order
  ↓
  Evento: order.placed disparado
  ↓
  ┌─────────────────────────────┐
  │ Subscriber escuta evento    │
  │ order-placed.ts executado   │
  └─────────────────────────────┘
  ↓
  Notification Module ativado
  ↓
  ┌─────────────────────────────┐
  │ 1. Renderiza template       │
  │    (react-email)            │
  │ 2. Envia via Resend/SendGrid│
  └─────────────────────────────┘
  ↓
Cliente recebe email de confirmação
```

### 4. Busca de Produtos
```
Cliente digita busca no Storefront
  ↓
  Storefront consulta MeiliSearch diretamente
  ↓
  ┌───────────────────────────────┐
  │ MeiliSearch retorna resultados│
  │ (indexados pelo backend)      │
  └───────────────────────────────┘
  ↓
  Resultados exibidos instantaneamente
  
  Em paralelo:
  Backend sincroniza produtos → MeiliSearch
  (quando produtos são criados/atualizados)
```

## 🗄️ Persistência de Dados

### Volumes Docker

```
┌──────────────────────────────────────────────────────────────┐
│                    Docker Volumes                            │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  postgres_data/                                              │
│  ├── base/                  ← Tabelas do banco              │
│  ├── global/                ← Configurações globais         │
│  └── pg_wal/                ← Write-Ahead Logs              │
│                                                              │
│  redis_data/                                                 │
│  └── dump.rdb               ← Snapshot do Redis             │
│                                                              │
│  minio_data/                                                 │
│  └── medusa-media/          ← Bucket com imagens            │
│      ├── {ulid}.jpg                                          │
│      ├── {ulid}.png                                          │
│      └── ...                                                 │
│                                                              │
│  meilisearch_data/                                           │
│  └── data.ms/               ← Índices de busca              │
│                                                              │
│  backend_uploads/                                            │
│  └── temp/                  ← Uploads temporários (fallback)│
│                                                              │
└──────────────────────────────────────────────────────────────┘

Localização no host:
  macOS/Linux: /var/lib/docker/volumes/
  Windows: C:\ProgramData\docker\volumes\
```

## 🌐 Rede e Comunicação

### Resolução de Nomes

Dentro da rede `medusa-network`, containers se comunicam por nome:

```
Container Backend quer acessar PostgreSQL:
  DATABASE_URL=postgres://user:pass@postgres:5432/medusa
                                      ↑
                              Nome do container
                              Docker DNS resolve automaticamente

Comunicação Interna:
┌─────────────┐     ┌──────────────┐     ┌─────────────┐
│  Backend    │────→│ Docker DNS   │────→│ PostgreSQL  │
│ 172.28.0.5  │     │              │     │ 172.28.0.2  │
└─────────────┘     └──────────────┘     └─────────────┘
      ↓
   postgres:5432 → 172.28.0.2:5432
```

### Portas Expostas

```
Host Machine                Docker Network (medusa-network)
                           
localhost:8000      →      storefront:8000
localhost:9000      →      backend:9000
localhost:9010      →      minio:9000
localhost:9011      →      minio:9001 (console)
localhost:7700      →      meilisearch:7700
localhost:5432      →      postgres:5432 (em dev)
localhost:6379      →      redis:6379 (em dev)

Em produção, apenas 8000 e 9000 são expostas
(através de reverse proxy com SSL)
```

## 🔐 Segurança em Camadas

```
┌─────────────────────────────────────────────────────────────┐
│ Layer 1: Network Isolation                                  │
│ • Containers isolados na rede medusa-network               │
│ • Comunicação apenas entre containers autorizados          │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ Layer 2: User Permissions                                   │
│ • Backend roda como usuário 'medusa' (UID 1001)           │
│ • Storefront roda como usuário 'nextjs' (UID 1001)        │
│ • Nenhum container roda como root                          │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ Layer 3: Environment Variables                              │
│ • Secrets não hardcoded                                    │
│ • .env não commitado no git                                │
│ • Suporte a Docker Secrets em produção                     │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ Layer 4: Health Checks                                      │
│ • Serviços com health checks automáticos                   │
│ • Restart automático em caso de falha                      │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ Layer 5: SSL/TLS (Produção)                                │
│ • Reverse proxy (Nginx/Traefik)                            │
│ • Certificados Let's Encrypt                               │
│ • HTTPS obrigatório                                        │
└─────────────────────────────────────────────────────────────┘
```

## 🚀 Startup Sequence

Ordem de inicialização controlada por `depends_on` e `health_check`:

```
1. ┌──────────────┐
   │  PostgreSQL  │  ← Inicia primeiro
   └──────┬───────┘
          │ Health check: pg_isready
          ↓
2. ┌──────────────┐
   │    Redis     │  ← Inicia em paralelo com Postgres
   └──────┬───────┘
          │ Health check: redis-cli ping
          ↓
3. ┌──────────────┐
   │    MinIO     │  ← Inicia em paralelo
   └──────┬───────┘
          │ Health check: curl /health
          ↓
4. ┌──────────────┐
   │ MinIO Setup  │  ← Cria bucket (executa uma vez)
   └──────┬───────┘
          │ Cria bucket e sai
          ↓
5. ┌──────────────┐
   │ MeiliSearch  │  ← Inicia em paralelo
   └──────┬───────┘
          │ Health check: wget /health
          ↓
   ┌──────────────────────────────────┐
   │ Todos os serviços estão healthy │
   └──────────────┬───────────────────┘
                  ↓
6. ┌──────────────┐
   │   Backend    │  ← Inicia após todos estarem prontos
   └──────┬───────┘
          │ Executa migrations
          │ Health check: wget /health
          ↓
7. ┌──────────────┐
   │  Storefront  │  ← Inicia por último
   └──────────────┘
          │ Aguarda backend estar healthy
          ↓
   ┌──────────────┐
   │ Sistema OK!  │
   └──────────────┘
```

## 📦 Build Process

### Backend Build Stages

```
Stage 1: base
┌─────────────────────┐
│ node:22-alpine      │
│ + pnpm 9.10.0       │
└──────────┬──────────┘
           ↓
Stage 2: deps
┌─────────────────────┐
│ Copia package.json  │
│ pnpm install        │
│ (~500MB)            │
└──────────┬──────────┘
           ↓
Stage 3: builder
┌─────────────────────┐
│ Copia src/          │
│ pnpm build          │
│ Gera .medusa/       │
└──────────┬──────────┘
           ↓
Stage 4: runner (FINAL)
┌─────────────────────┐
│ Apenas arquivos     │
│ necessários         │
│ • node_modules      │
│ • .medusa/          │
│ • src/ (runtime)    │
│ (~400MB)            │
└─────────────────────┘
```

### Storefront Build Stages

```
Stage 1: base
┌─────────────────────┐
│ node:22-alpine      │
│ + pnpm 9.10.0       │
└──────────┬──────────┘
           ↓
Stage 2: deps
┌─────────────────────┐
│ Copia package.json  │
│ pnpm install        │
└──────────┬──────────┘
           ↓
Stage 3: builder
┌─────────────────────┐
│ Copia src/          │
│ pnpm build:next     │
│ Gera .next/         │
│ com standalone      │
└──────────┬──────────┘
           ↓
Stage 4: runner (FINAL)
┌─────────────────────┐
│ Apenas standalone   │
│ • .next/standalone/ │
│ • .next/static/     │
│ • public/           │
│ (~250MB)            │
└─────────────────────┘
```

## 🔄 Development vs Production

### Development Mode
```
docker-compose.yml + docker-compose.override.yml

Features:
✅ Hot reload via volumes
✅ Source maps habilitados
✅ Debugging tools
✅ Portas todas expostas
✅ Logs verbosos

Volumes montados:
backend/src     → /app/src
storefront/src  → /app/src

Comando: make dev
```

### Production Mode
```
docker-compose.yml + docker-compose.prod.yml

Features:
✅ Código compilado
✅ Portas internas apenas
✅ Resource limits
✅ Restart policies
✅ Otimizações de performance

Sem volumes de código fonte

Comando: make prod
```

## 📊 Monitoramento de Recursos

```
┌─────────────────────────────────────────────────────────────┐
│                   Uso de Recursos (Estimado)                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  PostgreSQL                                                 │
│  ├─ CPU: ~10-20%                                            │
│  ├─ RAM: ~100-200MB                                         │
│  └─ Disk: Cresce conforme dados                             │
│                                                             │
│  Redis                                                      │
│  ├─ CPU: ~5-10%                                             │
│  ├─ RAM: ~50-100MB                                          │
│  └─ Disk: ~10-50MB                                          │
│                                                             │
│  MinIO                                                      │
│  ├─ CPU: ~5-15%                                             │
│  ├─ RAM: ~100-150MB                                         │
│  └─ Disk: Cresce conforme mídia                             │
│                                                             │
│  MeiliSearch                                                │
│  ├─ CPU: ~10-30%                                            │
│  ├─ RAM: ~200-500MB                                         │
│  └─ Disk: ~100-500MB                                        │
│                                                             │
│  Backend                                                    │
│  ├─ CPU: ~20-50%                                            │
│  ├─ RAM: ~500MB-1GB                                         │
│  └─ Disk: ~400MB (imagem)                                   │
│                                                             │
│  Storefront                                                 │
│  ├─ CPU: ~10-30%                                            │
│  ├─ RAM: ~200-500MB                                         │
│  └─ Disk: ~250MB (imagem)                                   │
│                                                             │
│  TOTAL MÍNIMO                                               │
│  ├─ CPU: 2 cores                                            │
│  ├─ RAM: 2GB                                                │
│  └─ Disk: 10GB                                              │
│                                                             │
│  RECOMENDADO PRODUÇÃO                                       │
│  ├─ CPU: 4 cores                                            │
│  ├─ RAM: 4-8GB                                              │
│  └─ Disk: 50GB+                                             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 🎯 Conclusão

Esta arquitetura fornece:
- ✅ **Isolamento** - Cada serviço em seu container
- ✅ **Escalabilidade** - Fácil adicionar réplicas
- ✅ **Portabilidade** - Roda em qualquer lugar com Docker
- ✅ **Desenvolvimento** - Hot reload e debugging
- ✅ **Produção** - Otimizado e seguro
- ✅ **Manutenção** - Fácil backup e restore
- ✅ **Monitoramento** - Health checks integrados

**Pronto para produção!** 🚀
